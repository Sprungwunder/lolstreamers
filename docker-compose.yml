services:
  django-api:
    build:
      context: .
      dockerfile: src/Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./src:/app/src
    networks:
      - web-app-network

networks:
  web-app-network:
    external: true
